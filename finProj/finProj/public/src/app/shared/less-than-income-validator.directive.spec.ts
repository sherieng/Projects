import { LessThanIncomeValidatorDirective } from './less-than-income-validator.directive';

describe('LessThanIncomeValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new LessThanIncomeValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
